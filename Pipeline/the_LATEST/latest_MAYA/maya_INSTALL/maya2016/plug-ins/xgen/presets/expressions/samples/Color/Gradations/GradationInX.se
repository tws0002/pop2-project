# Gradation In X
# You provide the loValue in X and the hiValue in X (world space units) and it will return
# a linear grad from white to black, in the X direction. 

$loValue =0.000;    #-20.0,20.0
$hiValue =1.000;    #-20.0,20.0
invert(linearstep($P[0],$loValue,$hiValue))
