$sizeU = 1;#1:10
$sizeV = 10.000;#1:10
$rangeMult = 100;
$sizeU = $sizeU * $rangeMult;
$sizeV = $sizeV * $rangeMult;
#sin($u*$sizeU) * sin($v*$sizeV)
cos($u*$sizeU) 



####
$sizeU = 1;#1:10
$sizeV = 1.45000;#1:10
$rangeMult = 100;
$sizeU = $sizeU * $rangeMult;
$sizeV = $sizeV * $rangeMult;
#sin($u*$sizeU) * sin($v*$sizeV)
(abs(cos($v*$sizeV)) + .25) * .75

