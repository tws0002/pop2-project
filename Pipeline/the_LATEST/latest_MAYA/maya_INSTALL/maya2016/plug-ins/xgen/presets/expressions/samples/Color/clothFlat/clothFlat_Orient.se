
### 
### vector 1, .5, .5  represents Y up  (shirt front, back) 
### vector 0, .5, .5  represents Y down 
### vector .5, 1, .5  represents X axis + (sleeves)
### vector .5, 0, .5  represents X axis -  (sleeves)
###


$vec =[0,0.5,0.5]; # 0.000, 1.000

$Cnew=norm([$vec[0] * $dPdv[1] - $vec[1] * $dPdv[0], $vec[1] * $dPdu[0] - $vec[0] * $dPdu[1], 0])/2+.5;

$vecFix = $Cnew*2-1;

norm($dPdu * $vecFix[0] + $dPdv * $vecFix[1])/2+.5
