# GradationInY With Curve
# You provide the loHeight value in Y and the hiHeight value in Y (world space units) and it will return
# a linear grad from black to white, bottom to top. Then it funnels that thru a curve to remap it.

$loHeight =-1.000;    #-20.0,200.0
$hiHeight =2.000;    #-20.0,200.0
$yGrad = linearstep($P[1],$loHeight,$hiHeight);
$sculptCurve =curve($yGrad,  0.000, 0.000, 4,  1.000, 1.000, 4);
$sculptCurve


