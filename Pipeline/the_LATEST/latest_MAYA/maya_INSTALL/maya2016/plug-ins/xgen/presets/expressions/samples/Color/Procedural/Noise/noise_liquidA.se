$Offset =0.22857;
$liquidScale =2.28571;#0,100.0
$zitsLo =0.15429;
$zitsHi =0.09143;
$allLo =0.10857;
$allHi =0.74857;
$noiseScaleFine =3.42857;#0,300.0
$noiseScaleLarge =0.57143;#0,100.0

$PP = ($P + $Offset) + noise($P*$liquidScale);
$nBuf = (fbm($PP * $noiseScaleFine)+ fbm($PP * $noiseScaleLarge)) / 2;
$zitsBuf  = smoothstep ($nBuf, $zitsLo, $zitsHi);
smoothstep ($zitsBuf + $nBuf,$allLo,$allHi)

