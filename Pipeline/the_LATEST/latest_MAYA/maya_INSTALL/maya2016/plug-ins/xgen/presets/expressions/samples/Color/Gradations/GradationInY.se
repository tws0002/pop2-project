# GradationInY
# You provide the loHeight value in Y and the hiHeight value in Y (world space units) and it will return
# a linear grad from white to black, bottom to top. 

$loHeight =0.000;    #-20.0,20.0
$hiHeight =1.000;    #-20.0,20.0
invert(linearstep($P[1],$loHeight,$hiHeight))
