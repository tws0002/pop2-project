#Generates a gradient based on distance from camera

$scale = 8;#1,100

$source = $cam;
$dist = dist( $source[0], $source[1], $source[2], $P[0],$P[1],$P[2])/($scale^2);
$col = ccurve($dist,1,[0,0.0470588,1],4,0,[1,0,0],4,0.47929,[0.14902,1,0],4);
$col
